<img class="avatar"
<%= image_tag @user.profilepic.url(:avatar) %><br>

<%= form_for @user, :html => { :multipart => true } do |f| %>
	<%= f.file_field :profilepic %>
	<%= f.submit 'Save a picture' %>
<% end %>
